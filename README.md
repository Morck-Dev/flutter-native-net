# flutter-native-net

Flutter 原生 HTTP 网络插件，基于 [libcurl](https://curl.se/libcurl/) —— 全球部署量超 200 亿次的 HTTP 引擎。

一套 C 代码覆盖 **Android (API 21+)、iOS (9.0+)、macOS (10.11+)、Linux、Windows (7+)**，Web 自动回退 Fetch API。

完整文档和使用示例见 [native_net/README.md](native_net/README.md)。
